# Archive

本目录仅保存废弃技术路线资料，作为业务参考，不再继续维护。

已归档：

- `legacy-nestjs-api-server`：旧 NestJS 后端源码与配置
- `legacy-nestjs-api-server-node-modules-residual`：旧后端依赖残留目录
- `legacy-postgresql-migrations`：旧 PostgreSQL Migration

V2 正式路线：

```text
Go + Gin + GORM + MySQL 8.4/InnoDB + Redis
API Prefix: /api/v2
```

server {
  listen 80;
  server_name localhost;

  location /health {
    access_log off;
    return 200 "ok\n";
    add_header Content-Type text/plain;
  }

  location / {
    return 200 "Yinghai local nginx is running.\n";
    add_header Content-Type text/plain;
  }
}

